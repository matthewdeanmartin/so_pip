# so these tools don't conflict with each other
pipx install isort
pipx install mypy
pipx install vulture

# in so_pip
#pipx install black
#pipx install pyupgrade
pipx install pylint
pipx install radon
pipx install bandit
